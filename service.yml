apiVersion: v1
kind: Service
metadata:
  name: student-reg-service
spec:
  selector:
    app: student-reg # Updated to match your deployment label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
